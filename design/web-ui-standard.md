# Web Vue UI 标准（视觉真源）

**后续所有 Web 页面都必须按本文设计和实现。**

| 跟谁 | 管什么 |
|---|---|
| `design/img.png` | 骨架：通顶深色侧栏 + 浅内容、绿胶囊导航、白卡片大圆角、每页一颗主按钮 |
| Apple Store 官网 | 字体（系统 SF / 苹方）、字色、浅灰页底、暗色近黑、可读性 |
| `oci-start-web/src/styles/tokens.css` | 唯一 token 真源。页面禁止再写死另一套蓝紫/字号/字色 |

easy-dev 的 Oracle Redwood、`header-first` 顶栏、禁止阴影、6px 圆角 **全部不作 Web 依据**。  
业务菜单、接口、文案仍是 oci-start 自己的；图里的 “Nexora / Jordan” 只是气质，不抄假数据。  
**只改样式/布局，不改、不漏逻辑和接口。**

---

## 气质

浅色工作台 + 通顶深色侧栏。  
白卡片、大圆角、轻阴影、森林绿主操作。  
字像 Apple Store：清晰、近黑墨色、系统字体。克制、干净、留白充足。

---

## 骨架（全站同一套壳）

```
┌──────────┬─────────────────────────────────────────────┐
│ 深色侧栏  │  顶栏：搜索（左） · 通知/日期 · 用户（右）     │
│ 通顶     │─────────────────────────────────────────────│
│ Logo     │  内容：业务统计或列表卡片，直接开始            │
│ 导航     │  卡内：搜索/筛选 · 必要操作 + 主按钮            │
│ 氛围区   │  白卡片网格，间距 16–20px                    │
│ 用户卡片 │                                             │
└──────────┴─────────────────────────────────────────────┘
```

1. **侧栏通顶**（不是 easy-dev 那种顶栏先横贯再侧栏）。宽约 **220px**，折叠约 64px。
2. **内容区浅底**，自己带一条矮顶栏（搜索 + 工具 + 头像），不压在侧栏上面。
3. **内容直接开始**（2026-09-13 用户锁定）：已完成页面和后续所有迁移页面都不再重复展示页面大标题、欢迎语或装饰说明。必要操作并入现有卡片工具栏，最多 **一个** 实心主按钮（森林绿）；页内业务模块标题保留。
4. **内容全部进白卡片**，不要通栏裸表、不要 iframe 感的灰块堆砌。
5. **Web 铺满浏览器视口**：侧栏贴左、主区贴右，无外壳 padding、无整窗外圆角。图上的灰衬底 + 大圆角是 Figma 展示框，只给 Mac 系统窗口用，不要抄进浏览器。

侧栏结构（对 oci-start 菜单，不改分组顺序）：

- 顶：Logo 叶标 + 产品名 + 一句副文案
- 中：导航。当前项 = **绿色圆角胶囊**（白字）；其它项 = 浅色图标 + 灰白字
- 下：可选氛围图 + 一句短文案（可用渐变代替版权图）
- 底：当前用户（头像 + 名 + 角色）

---

## 字体（Apple Store）

```
-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display",
"PingFang SC", "Hiragino Sans GB", "Noto Sans SC", "Microsoft YaHei",
"Helvetica Neue", Helvetica, Arial, sans-serif
```

- 禁止 Avenir Next / Inter / Roboto 打头（中英混排发糊）。
- **字号按当前 Vue OCI 实例列表统一（2026-09-13 用户指定）**：正文、表格主文本/表头、按钮、输入框与表单标签 **14px**；说明、副行与页脚 **13px**；紧凑徽标 **12px**；业务模块标题 **16px**；弹窗标题 **18px**。正文行高仍约 1.47，标题字重 **600**。统计大数字保持各图表既有层级。
- 统一使用 `--font-size-body/secondary/caption/section/dialog-title`，值只在 `tokens.css` 定义。不得为了紧凑布局再把主文本缩至 11–13px，`size="small"` 只控制组件高度/间距，不降低文字字号；手机断点也沿用同一字级。
- 本轮覆盖已完成 A1–A7、共享导航与相关弹层；已认可的独立登录原型和 Canvas 地图绘制尺寸遵循各自专项，不受控制台字号规则改写。
- `font-synthesis: none`；`-webkit-font-smoothing: antialiased`；`-moz-osx-font-smoothing: grayscale`。
- 表格/表单/按钮必须继承 `--sans` 和 `--text-*`，不要 Element Plus 默认灰字。

## 颜色

写进 `oci-start-web/src/styles/tokens.css`。组件禁止再写死蓝紫渐变或另一套字色。

| Token | 值 | 用途 |
|---|---|---|
| `--bg-app` | `#f5f5f7` | mockup 衬底，浏览器不用 |
| `--bg-page` | `#f5f5f7` | 内容区底（Apple Store 浅灰） |
| `--bg-sidebar` | `#1d1d1f` | 侧栏底（Apple 近黑） |
| `--bg-card` | `#ffffff` | 卡片 |
| `--bg-search` | `#f5f5f7` | 顶栏搜索胶囊 |
| `--text-primary` | `#1d1d1f` | 标题、数字（Apple ink） |
| `--text-secondary` | `#6e6e73` | 说明、表头 |
| `--text-on-dark` | `#f5f5f7` | 侧栏文字 |
| `--text-on-dark-muted` | `#a1a1a6` | 侧栏未选 |
| `--brand` | `#1b8a6a` | 主按钮、进度、当前导航 |
| `--brand-hover` | `#157456` | 主按钮悬停 |
| `--brand-deep` | `#0d4d3f` | 导航选中胶囊偏深绿 |
| `--nav-active-fg` | `#ffffff` | 选中导航字 |
| `--text-muted` | `#86868b` | 更弱的辅助字 |
| `--border` | `#d2d2d7` | 卡片/表线 |
| `--shadow-card` | `0 2px 12px rgba(0,0,0,.04)` | 轻阴影 |
| `--status-ok` | `#1b8a6a` | 成功 |
| `--status-ok-bg` | `#e8f6f1` | 绿标签底 |
| `--status-info` | `#0071e3` | 信息（图表蓝，不是品牌） |
| `--status-warn` | `#b45309` | 警告字 |
| `--status-danger` | `#e24b4a` | 失败 |
| `--btn-dark` | `#1d1d1f` | 深色次按钮 |

暗色（`[data-theme=dark]`）：

| Token | 值 |
|---|---|
| `--bg-page` / `--bg-sidebar` | `#000000` |
| `--bg-card` | `#1d1d1f` |
| `--bg-search` / `--bg-hover` | `#2c2c2e` |
| `--text-primary` | `#f5f5f7` |
| `--text-secondary` | `#a1a1a6` |
| `--text-muted` | `#6e6e73` |
| `--border` | `#424245` |

主按钮仍是森林绿 `#1b8a6a`，不要改成 Apple 链接蓝 `#0066cc`。

---

## 尺寸与形状

| 元素 | 规则 |
|---|---|
| 卡片圆角 | **16–20px**（大圆角，不是 6px） |
| 按钮 / 搜索 / 导航胶囊 | **999px 或 12px**，主按钮偏胶囊 |
| 侧栏宽 | 220px；折叠 64px |
| 内容内边距 | 24–32px |
| 卡片间距 | 16–20px |
| 顶栏高 | 约 56px |
| 内容区页标题 | 不重复展示页面名称；操作就近并入卡片工具栏 |
| 表行高 | 约 48–52px |
| 阴影 | 卡片轻阴影，禁止重投影、禁止毛玻璃乱飞 |
| KPI 图标 | 40px 圆底 + 单色图标，不要彩色胶囊标签当类型 |

---

## 组件怎么用这张图

| 场景 | 对图上的哪一块 |
|---|---|
| 仪表盘 | 业务 KPI 直接置顶，下方监控卡片网格；不加欢迎行或重复页标题。数据用现有 `/boot/dashboard-stats` 等，不编造 |
| 列表页（租户/实例/开机） | 同一壳；主区 **一张** 白卡片：一条紧凑搜索/操作栏后直接接表格，底部分页。不要叠加装饰标题、说明或当前页统计；列表使用剩余高度，表头和分页保持可见 |
| 设置页 | 白卡片网格，两列等宽，不要大小不一 |
| 主按钮 | 只有一个森林绿实心（新建/保存类） |
| 次操作 | 白底描边，或图中 Quick Actions 那种深色胶囊（批量/危险需另开确认，逻辑不变） |
| 状态 | 小圆点 + 浅底标签（On Track 绿 / In Progress 蓝 / At Risk 红），不要彩虹胶囊满屏 |
| 图表 | 绿 + 蓝两条，浅网格，圆环用状态色 |

---

## 区域地图专项

用户最终效果参考 `design/dot-world-map.html`（2026-09-13），随后明确去掉立体地球、只保留平铺并增加原型动态连线。使用 Canvas 2D 点阵地图，深/浅配色、点阵密度、发光标记、脉冲、细弧线和流动光点按稿实现；连线默认开启且可独立关闭。这是图内可视化风格，不改变控制台全站字体/布局。真实 ARM 与我的区域同时展示，交集区分；连线仅为用户要求的视觉动效，不引入示例数据或拓扑/流量等业务含义。地图全宽展示、列表保持轻表头与现有业务操作；所有控制和提示国际化。

## 登录页专项

2026-09-13 用户最新指定 `design/oci-start-login.html`，覆盖此前大叶形 Logo 登录方案。左侧小品牌、文案和平铺点阵地图，右侧紧凑登录表单；地图添加动态弧线与流动光点。登录页局部 tokens 按稿提供深浅两套色值，保留系统字体与绿色主操作。现有服务端登录、二验、注册、找回密码、OAuth 条件与跨端 RSA 抓取接口保持；原型仅用作视觉与动效参考，不引入模拟请求或虚假状态。

用户进一步要求完全按原型的布局和样式实现，登录注册逻辑保持此前实现。主 CSS 直接使用原型原文，恢复同款 DOM 层级、默认行高、字体列表、表单总宽/内边距和断点，不自行优化尺寸或添加另一套布局。原型缺少的真实业务流程通过局部兼容样式接入，不改主表单规则。

---

## 国际化

所有 Vue 迁移页面同步补齐中英文，不只处理菜单。工具栏、表头、菜单、弹层、表单、状态/空态、校验/确认/反馈、placeholder、title、aria 文案都进语言包。动态标题、选项和本地持久错误在渲染或 computed 中翻译，日期/时间/数字随语言更新。按枚举 code 翻译展示值，保留真实用户数据/服务端文本与请求契约。英文布局允许合理换行、表格横向滚动。

---

## 明确不要

- 不要 easy-dev 红木 `#C74634`、不要「全站禁用阴影」
- 不要现在 FTL 默认的通体深色 + iframe
- 不要顶栏横贯全窗再在下面挂侧栏（header-first）
- 不要抄图里的假项目名、假用户、山景版权图（氛围用渐变/几何即可）
- 不要为了好看改接口或字段、不要漏掉旧页操作
- 不要把 mockup 的外灰边和整窗外圆角抄进浏览器（那是展示框；Web 整屏，Mac 才有系统窗口圆角）
- 卡片 / 按钮 / 导航胶囊的内部圆角要保留
- 不要 Avenir Next 打头、不要青灰暗色底、不要页面私写字色
- 不要 Apple 链接蓝当主色

---

## 验收

对照 `design/img.png`：侧栏深、内容浅、当前导航绿胶囊、主按钮绿、白卡片大圆角、页头左标题右一颗主按钮。  
对照 Apple Store：字清晰、浅色近黑墨、暗色近黑底白字。Web 铺满视口。颜色/字体只从 token 来。  
亮、暗各看一遍：正文、表头、输入框、分页都不能发糊或糊成一片。
